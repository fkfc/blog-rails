<% 
  comment_props = {
    id: comment.id,
    content: comment.content,
    createdAt: comment.created_at,
    postCommentPath: post_comment_path(comment.post, comment) 
  }
%>
<%= react_component("Comment", props: comment_props, prerender: false) %>
