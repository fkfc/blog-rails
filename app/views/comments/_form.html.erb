<%= form_for([@post, @comment]) do |f| %>

<%= render 'shared/comment_error_messages' %>

<%= f.text_area :content, size: "60x12", placeholder: "Type your comment here" %> <br>

<%= f.submit %>

<% end %>
