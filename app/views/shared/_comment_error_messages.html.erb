<% if @comment.errors.any? %>
  <%
    props = {
      name: "comment",
      errors: @comment.errors.full_messages
    }
  %>
  <%= react_component("ErrorList", props: props, prerender: false) %>
<% end %>
