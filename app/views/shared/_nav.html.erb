<%
  props = {
    signedIn: user_signed_in?,
    currentUserEmail: user_signed_in? ? current_user.email : nil,
    signOutLink: destroy_user_session_path,
    signInLink: new_user_session_path,
    signUpLink: new_user_registration_path,
  }
%>
<%= react_component("NavBar", props: props, prerender: false) %>
